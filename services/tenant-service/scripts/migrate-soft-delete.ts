// Soft delete migration script removed
// The project maintainer requested soft-delete migrations be deleted.
// This script is retained as a harmless stub to avoid breaking any callers.

console.log('Soft delete migrations have been removed from this codebase.');
process.exit(0);
