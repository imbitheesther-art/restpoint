{
  "name": "@montezuma/shared-services",
  "version": "1.0.0",
  "description": "Shared services (file storage, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "dev": "echo 'Shared services package ready'"
  },
  "dependencies": {
    "amqplib": "^0.10.7",
    "mysql2": "^3.22.6",
    "redis": "^4.6.12",
    "yallist": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  }
}