{
  "name": "@montezuma/shared-services",
  "version": "1.0.0",
  "description": "Shared services (file storage, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "amqplib": "^0.10.7",
    "redis": "^4.6.12"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "rimraf": "^6.0.1"
  }
}