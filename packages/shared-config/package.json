{
  "name": "@montezuma/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration for database, tenancy, and environment setup",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "prepublishOnly": "npm run clean && npm run build",
    "dev": "echo 'Shared config package ready'"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "rimraf": "^6.0.1"
  }
}