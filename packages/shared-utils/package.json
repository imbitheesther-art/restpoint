{
  "name": "@montezuma/shared-utils",
  "version": "1.0.0",
  "description": "Shared utility functions (time, formatting, helpers)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "dev": "echo 'Shared utils package ready'"
  },
  "dependencies": {
    "luxon": "^3.4.1"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.0",
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "rimraf": "^6.0.1"
  }
}